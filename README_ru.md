```
╔══════════════════════════════════════════════════════════╗
║                                                          ║
║     ██████╗  ██████╗ ████████╗██████╗                    ║
║    ██╔════╝ ██╔═══██╗╚══██╔══╝██╔══██╗                   ║
║    ██║  ███╗██║   ██║   ██║   ██████╔╝                   ║
║    ██║   ██║██║   ██║   ██║   ██╔══██╗                   ║
║    ╚██████╔╝╚██████╔╝   ██║   ██║  ██║                   ║
║     ╚═════╝  ╚═════╝    ╚═╝   ╚═╝  ╚═╝                   ║
║                                                          ║
║           CLI Client for TestRail API v2                 ║
║                                                          ║
╚══════════════════════════════════════════════════════════╝
```

# gotr — CLI-клиент для TestRail API

[English](README.md) | [Русский](README_ru.md)

[![Version](https://img.shields.io/badge/version-2.3.0-blue.svg)](CHANGELOG.md)
[![Go Version](https://img.shields.io/badge/go-1.24.1-blue.svg)](go.mod)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

`gotr` — удобная и мощная утилита командной строки для работы с TestRail API v2.  
Позволяет выполнять GET/POST-запросы, экспортировать/импортировать данные, синхронизировать сущности между проектами, управлять тест-ранами и результатами, фильтровать ответы через встроенный `jq` и многое другое — без необходимости устанавливать внешние зависимости.

> **Текущая версия: 2.3.0** — Подробности в [CHANGELOG](CHANGELOG.md)

## 🙏 Используемые библиотеки

Проект использует следующие замечательные open-source библиотеки:

- **[spf13/cobra](https://github.com/spf13/cobra)** — фреймворк для CLI приложений
- **[spf13/viper](https://github.com/spf13/viper)** — конфигурация и env-переменные
- **[cheggaaa/pb/v3](https://github.com/cheggaaa/pb)** — прогресс-бары
- **[go.uber.org/zap](https://github.com/uber-go/zap)** — высокопроизводительное логирование
- **[stretchr/testify](https://github.com/stretchr/testify)** — тестирование
- **[embedded jq](https://github.com/itchyny/gojq)** — встроенная утилита jq для фильтрации JSON

## 📁 Структура проекта

```bash
gotr/
├── cmd/                    # Команды CLI
│   ├── get/               # GET команды (cases, suites, projects и т.д.)
│   ├── sync/              # SYNC команды (миграция данных)
│   ├── commands.go        # Централизованная регистрация команд
│   ├── root.go            # Root команда и конфигурация
│   ├── config.go          # Команды управления конфигом
│   ├── list.go            # Команда list
│   └── ...                # Другие команды
├── docs/                   # Документация
│   ├── installation.md
│   ├── configuration.md
│   ├── get-commands.md
│   ├── sync-commands.md
│   └── ...
├── embedded/               # Встроенные утилиты (jq)
├── internal/               # Внутренние пакеты
│   ├── client/            # HTTP клиент для TestRail API
│   ├── migration/         # Логика миграции (sync)
│   ├── models/            # Структуры данных
│   └── utils/             # Утилиты
├── pkg/                    # Публичные пакеты
├── main.go                 # Точка входа
├── go.mod                  # Go модули
└── Makefile               # Сборка
```

## 🚀 Быстрый старт

```bash
# Установка (Linux/macOS)
curl -s -L https://github.com/Korrnals/gotr/releases/latest/download/gotr-$(uname -s | tr '[:upper:]' '[:lower:]')-amd64 -o gotr && chmod +x gotr && sudo mv gotr /usr/local/bin/

# Проверка
gotr --help
```

## ✨ Основные возможности

- 📡 **Полная поддержка TestRail API** — GET-запросы ко всем эндпоинтам
- 🔄 **Синхронизация** — миграция cases, shared steps, suites, sections между проектами
- 🎯 **Интерактивный режим** — не нужно запоминать ID проектов и сьютов
- 📦 **Встроенный jq** — фильтрация без установки внешних утилит
- 💾 **Экспорт** — сохранение данных в JSON с автоматическим именованием
- 🔧 **Гибкая конфигурация** — флаги, env-переменные, конфиг-файл
- 🖥️ **Автодополнение** — bash/zsh/fish completion

## 📚 Документация

Подробная документация находится в директории [`docs/`](docs/):

- [Установка](docs/installation.md)
- [Конфигурация](docs/configuration.md)
- [Команды GET](docs/get-commands.md)
- [Команды SYNC](docs/sync-commands.md)
- [Интерактивный режим](docs/interactive-mode.md)
- [Другие команды](docs/other-commands.md)

## 🎮 Примеры использования

### Интерактивный режим

```bash
# Получение кейсов — выбор проекта и сьюта из списка
gotr get cases

# Синхронизация кейсов — выбор source и destination
gotr sync cases

# Полная миграция
gotr sync full
```

### Получение данных

```bash
# Все проекты
gotr get projects

# Кейсы проекта (с интерактивным выбором сьюта)
gotr get cases 30

# Или с явным указанием сьюта
gotr get cases 30 --suite-id 20069

# Все кейсы из всех сьютов проекта
gotr get cases 30 --all-suites

# Shared steps
gotr get sharedsteps 30
```

### Синхронизация

```bash
# Полная миграция (shared steps + cases)
gotr sync full \
  --src-project 30 --src-suite 20069 \
  --dst-project 31 --dst-suite 19859 \
  --approve --save-mapping

# Только shared steps
gotr sync shared-steps \
  --src-project 30 --dst-project 31 \
  --approve --save-mapping

# Только cases (с mapping-файлом)
gotr sync cases \
  --src-project 30 --src-suite 20069 \
  --dst-project 31 --dst-suite 19859 \
  --mapping-file mapping.json --approve
```

### Сравнение проектов

```bash
# Сравнение кейсов двух проектов
gotr compare cases --pid1 30 --pid2 31 --field title
```

### Фильтрация через jq

```bash
# Только id и название проектов
gotr get projects --jq --jq-filter '.[] | {id: .id, name: .name}'

# Красивый вывод с jq
gotr get case 12345 --jq
```

## ⚙️ Конфигурация

Приоритет конфигурации (от высшего к низшему):

1. **Флаги** (`--url`, `--username`, `--api-key`)
2. **Env-переменные** (`TESTRAIL_BASE_URL`, `TESTRAIL_USERNAME`, `TESTRAIL_API_KEY`)
3. **Конфиг-файл** (`~/.gotr/config.yaml`)

```bash
# Создать конфиг
gotr config init

# Просмотреть
gotr config view
```

## 🆕 Что нового

### 2026-02-03 — Интерактивный режим

- **Интерактивный выбор** для всех команд `get` и `sync` — не нужно запоминать ID
- **Автоматический выбор** единственного сьюта в проекте
- **Флаг `--all-suites`** для получения кейсов из всех сьютов
- **Реструктуризация** пакета `cmd/` — улучшена организация кода

### 2026-01-24 — Sync команды

- Новые команды `sync suites` и `sync sections`
- Унификация флагов для всех `sync/*` команд
- Unit-тесты для синхронизации

### 2026-01-15 — Get команды v2.0

- Переработанная команда `get` с подкомандами
- Позиционные аргументы для ID
- Улучшенная типизация (int64)

## 📦 Установка

См. [docs/installation.md](docs/installation.md)

## 🤝 Участие в проекте

Issues и Pull Requests приветствуются!

## 📄 Лицензия

MIT License — см. [LICENSE](LICENSE)
