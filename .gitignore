# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
godocker

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

**.go.*

gotr

*go.old*
*.old*
.testrail/
Notify.md
Tech.md

# Build artifacts
gotr-linux-amd64
gotr-darwin-amd64
gotr.exe

# часть расширения - должна быть в коде
!jq-linux-amd64
!jq-macos-amd64
!jq-windows-i386.exe.history/
.history/

# System development files (not for git)
# Contains: API_ANALYSIS.md, API_AUDIT.md, CHECKLIST.md, PLAN.md, AGENTS.md
.systems/

# Compressed binaries (for releases)
*.gz
*.zip
*.tar

# Distribution directory (contains built binaries and release artifacts)
dist/
