# План разработки gotr

## Техдолг (TODO)

### 1. Attachments API - неполная реализация
**Статус:** 🔴 Требует проверки и доработки

**Проблема:** В текущей реализации `cmd/attachments/` доступны только методы `add_attachment_to_*`:
- ✅ `add_attachment_to_case/{case_id}`
- ✅ `add_attachment_to_plan/{plan_id}`
- ✅ `add_attachment_to_plan_entry/{plan_id}/{entry_id}`
- ✅ `add_attachment_to_result/{result_id}`
- ✅ `add_attachment_to_run/{run_id}`

**Отсутствуют:**
- ❓ `get_attachments_for_case/{case_id}`
- ❓ `get_attachments_for_plan/{plan_id}`
- ❓ `get_attachments_for_plan_entry/{plan_id}/{entry_id}`
- ❓ `get_attachments_for_run/{run_id}`
- ❓ `get_attachments_for_test/{test_id}`
- ❓ `get_attachment/{attachment_id}`
- ❓ `delete_attachment/{attachment_id}`

**Действия:**
1. Проверить, доступны ли эти методы в TestRail API v2
2. Проверить, есть ли они в `internal/client/interfaces.go` в интерфейсе `AttachmentsAPI`
3. Если доступны - реализовать соответствующие CLI команды в `cmd/attachments/`
4. При реализации добавить команды `get` и `delete` (возможно, подкоманды как у `add`)

**Ссылка:** https://support.testrail.com/hc/en-us/articles/7077723756044-Attachments

---

## Статус этапов

### Stage 5: CLI Commands for All API Endpoints

| Шаг | Ресурс | Статус | Покрытие |
|-----|--------|--------|----------|
| 1 | Attachments | 🟡 Частично | Только add команды |
| 2 | Labels | ✅ Готово | 100% |
| 3 | Cases | ✅ Готово | 100% |
| 4 | Plans | ✅ Готово | 100% |
| 5 | Milestones | ✅ Готово | 100% |
| 6 | Configurations | ✅ Готово | 100% (полный CRUD) |
| 7 | Groups | ✅ Готово | 100% |
| 8 | Datasets | ✅ Готово | 100% (полный CRUD) |
| 9 | Variables | ⏳ Ожидает | - |
| 10 | BDDs | ⏳ Ожидает | - |
| 11 | Tests | ⏳ Ожидает | - |

---

## Конвенции разработки

### Структура CLI команд
- Каждый ресурс в своём пакете: `cmd/<resource>/`
- Функция создания команды: `new<Command>Cmd(getClient GetClientFunc) *cobra.Command`
- Эндпоинт API указывается в комментарии к функции
- Тесты в `*_test.go` файлах с 100% покрытием

### Требования к тестам
- Мок клиент через `test_helper.go`
- Тесты dry-run для модифицирующих операций
- Тесты валидации аргументов
- Тесты ошибок API
- Тесты записи в файл (`-o` флаг)

### Локализация
- Все команды и описания на русском языке
- Примеры использования в help
- Подробное описание в `Long` поле cobra.Command
